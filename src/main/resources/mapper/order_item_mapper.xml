<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gaoyifeng.aioserver.infrastructure.dao.mapper.OrderItemMapper">

    <!-- 结果映射 -->
    <resultMap id="OrderItemResultMap" type="com.gaoyifeng.aioserver.infrastructure.dao.po.OrderItemPO">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="quantity" property="quantity"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="total_price" property="totalPrice"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 插入订单项 -->
    <insert id="insert" parameterType="com.gaoyifeng.aioserver.infrastructure.dao.po.OrderItemPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_items (order_id, product_id, product_name, quantity, unit_price, total_price)
        VALUES (#{orderId}, #{productId}, #{productName}, #{quantity}, #{unitPrice}, #{totalPrice})
    </insert>

    <!-- 根据订单ID查询订单项列表 -->
    <select id="selectByOrderId" parameterType="string" resultMap="OrderItemResultMap">
        SELECT id, order_id, product_id, product_name, quantity, unit_price, total_price, create_time
        FROM order_items
        WHERE order_id = #{orderId}
        ORDER BY id
    </select>

    <!-- 批量插入订单项 -->
    <insert id="batchInsert" parameterType="list">
        INSERT INTO order_items (order_id, product_id, product_name, quantity, unit_price, total_price)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.quantity}, #{item.unitPrice}, #{item.totalPrice})
        </foreach>
    </insert>

</mapper>